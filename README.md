# About
This repository is part of the Rust programming language course.

## Activities
The activities can be ran by executing `cargo run --bin a1` in your terminal, where `a1` is the name of the activity file.

